int main()
{
    (a)
    do{                                                   //add {}
        int v1, v2;
        cout<<"please enter two numbers to sum: ";
        if(cin>>v1>>v2)
            cout<<"sum is: "<<v1+v2<<endl;
    }
    while(cin);
    
    (b)
    int ival;
    do{
        
    }while(ival=get_response());                        //ival不能初始化在while條件式
    
    (c)
    int ival;
    do{
        ival=get_response();                            //ival初始化在外面, 在do裡面每一次迴圈會更新
    }
    while(ival);

    return 0;
}
