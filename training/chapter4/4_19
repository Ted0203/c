int main()
{
    int *ptr;
    vector<int> vec;
    int ival;
    
    (a)
    ptr!=0&&*ptr++              //OK
    
    (b)
    ival++&&ival                //OK
    
    (c)
    vec[ival++]<=vec[ival]      //error: <=沒有順序, 應改為vec[ival] <= vec[ival+1]

    return 0;
}
