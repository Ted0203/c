int main()
{
    constexpr size_t array_size=10;
    int ia[array_size];
    for(size_t ix=1;ix<=array_size;++ix)        //because ia[10], from 0~9, ix from 1~10越界了
        ia[ix]=ix;
    
    //for(size_t ix=0;ix<array_size;++ix)
    //    ia[ix]=ix;
    
    return 0;
}
