#include <stdio.h>
#include <stdlib.h>
#define size 10

int main()
{
    int a=640, arr[size]={0}, i;

    for(i=0;i<size;i++)
    {
        arr[i]=a%2;
        a/=2;
    }

    for(i=size-1;i>=0;i--)
        printf("%d", arr[i]);

    return 0;
}

//output:
1010000000
