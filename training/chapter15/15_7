#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int a=1011011, sum=0, b=0, c;

    while(a>0)
    {
        c=a%10;
        a/=10;
        sum+=c*pow(2,b);
        b++;
    }
    printf("%d", sum);

    return 0;
}

//output:
91
